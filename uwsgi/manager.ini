[uwsgi]

project = manager
base = /home/django/manager

chdir = %(base)/%(project)
home = /root/.pyenv/versions/%(project)
module = %(project).wsgi:application

master = true
processes = 2

socket = %(base)/%(project)/%(project).sock
chmod-socket = 664
vacuum = true